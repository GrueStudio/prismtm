{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "global-projects-v0.0.0.json",
  "title": "Prism Global Projects Schema v0.0.0",
  "description": "Schema for a user-wide list of projects tracked by Prism.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name of the project."
      },
      "path": {
        "type": "string",
        "description": "The absolute file path to the project directory."
      },
      "version": {
        "type": "string",
        "description": "The schema version the project's files are currently using."
      },
      "created_at": {
        "type": "string",
        "description": "The timestamp when the project was created."
      }
    },
    "required": ["name", "path", "version", "created_at"]
  }
}
